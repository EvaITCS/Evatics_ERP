# Copy to .env for local development.
# Base URL of the backend API (Spring Boot). Must be prefixed REACT_APP_ for
# Create React App to inline it into the browser bundle at build time.
REACT_APP_BACKEND_API_URL=http://localhost:8080