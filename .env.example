MAIL_USERNAME=
MAIL_PASSWORD=
# SMTP server matching the mailbox provider (Microsoft 365: smtp.office365.com, Gmail: smtp.gmail.com)
MAIL_HOST=smtp.office365.com
MAIL_PORT=587

# Database
DATABASE_USER=root
DATABASE_PASSWORD=root
DATABASE_HOST=localhost
DATABASE_PORT=3306
DATABASE_NAME=lms_erp

# Admin init
ADMIN_TEMP_USERNAME=
ADMIN_TEMP_PASSWORD=

JWT_SECRET=

# Docker Compose only: host ports and the browser-facing backend URL baked
# into the frontend build. Defaults match local `npm start`/`mvnw` usage.
# BACKEND_PORT=8080
# FRONTEND_PORT=3000
# REACT_APP_BACKEND_API_URL=http://localhost:8080

# Terraform
TF_VAR_project=lms-erp
TF_VAR_region=us-east-1
TF_VAR_github_org=EvaITCS
TF_VAR_github_repo=lms-erp

# AWS
AWS_ACCOUNT_ID=065306944844
AWS_REGION=us-east-1
